#include "main.h"

/**
 * mul - function that multiplies two integers.
 *
 * @a: the integer to multiply with b
 * @b: the integer to multiply with a
 *
 * Return: the value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
